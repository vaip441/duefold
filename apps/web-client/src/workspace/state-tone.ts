/**
 * How a state word is reinforced wherever `.df-state` renders one. The word always
 * carries the meaning; the tone only echoes it, and green is never a tone.
 */
export type StateTone = 'neutral' | 'caution' | 'problem' | 'ready';
